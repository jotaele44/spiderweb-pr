# PRUAP Social Index — Cloud Sweep System

This project collects all anomalous Reddit posts related to UFO/UAP/USO activity around the Puerto Rico archipelago.

It runs easily inside **GitHub Codespaces**, even from iOS.

---

## 1. Setup (inside Codespaces)

Install dependencies:

```bash
pip install -r requirements.txt
