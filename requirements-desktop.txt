# Dependencies for the double-click desktop wrapper (see desktop/).
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pywebview>=6.2.1,<7
# server/backend/main.py imports these directly; they used to come in via the
# `-e .[server]` editable install, which desktop/setup.py no longer runs.
sse-starlette>=2.1,<4
aiosqlite>=0.20,<1
# Shared federation runtime pinned to an immutable TheHub commit so this
# repository installs from an isolated clone.
prii-desktop @ git+https://github.com/jotaele44/thehub-pr.git@f2b81769924689b4d959554928810b1d7b7ef3d6#subdirectory=packages/prii_desktop
