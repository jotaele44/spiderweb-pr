# Puerto Rico Airspace Intelligence System — Dependencies

# ── REQUIRED (Phase 0 baseline) ────────────────────────────────────────────
# Core image processing and OCR
opencv-python>=4.8
pytesseract>=0.3.10
Pillow>=10.0
numpy>=1.26

# ── REQUIRED (integration hardening) ───────────────────────────────────────
jsonschema>=4.17       # schema validation for all exports
pyarrow>=14.0          # parquet export for PR Intel adapter
pytest>=7.4            # test suite

# ── OPTIONAL (Phase 1 hardening — heavy ML engines) ────────────────────────
# Install one or both for ensemble OCR consensus (significant accuracy boost).
# These require substantial disk space and download time.
#
# PaddleOCR (requires paddlepaddle first):
#   pip install paddlepaddle>=2.6
#   pip install paddleocr>=2.7
#
# EasyOCR (requires PyTorch):
#   pip install torch>=2.0
#   pip install easyocr>=1.7

# ── OPTIONAL (dashboard) ───────────────────────────────────────────────────
# The React dashboard (dashboard.jsx) uses npm packages, not pip:
#   npm install lucide-react
#   Requires React 16.8+ and Tailwind CSS v3+

# ── LLM PIPELINE (optional) ─────────────────────────────────────────────────
# RAG index + local LLM query over PRUAP social data (prepare_data.py,
# rag_pipeline.py, query_llm.py)
praw>=7.8.1
chromadb>=0.4.0
sentence-transformers>=2.7.0
transformers>=4.40.0
torch>=2.2.0
accelerate>=0.27.0
