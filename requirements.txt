# Spiderweb — combined runtime dependencies.
#
# Dependencies are defined as extras in pyproject.toml. This file installs the
# airspace core plus the optional RAG and EarthGPT subsystems:
#
#   pip install -r requirements.txt
#   pip install -c constraints.txt -r requirements.txt   # exact pins
#
# For a single subsystem use the per-feature shim instead:
#   requirements-airspace.txt   requirements-gebco.txt
#   requirements-rag.txt        requirements-earthgpt.txt
#
# The PRIIS FastAPI backend is a separate extra:  pip install -e ".[server]"
# The React dashboard uses npm, not pip:  npm install lucide-react
#
# System prerequisite (not pip-installable):
#   Ubuntu:  sudo apt-get install tesseract-ocr
#   macOS:   brew install tesseract
-e .[airspace,rag,earthgpt]
