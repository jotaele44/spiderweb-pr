# FastAPI runtime only. Keep these ranges aligned with the ``server`` extra in
# pyproject.toml so the GUI parity workflow can boot the production API without
# installing Spiderweb's unrelated scientific and model stacks.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
sse-starlette>=2.1,<4
aiosqlite>=0.20,<1
pydantic>=2.7,<3
