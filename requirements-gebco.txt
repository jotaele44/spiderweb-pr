# GEBCO 2023 Bathymetry Pipeline
# Dependencies are the shared scientific stack (pyproject.toml `dependencies`)
# plus the `gebco` extra.
#
#   pip install -r requirements-gebco.txt
#   pip install -c constraints.txt -r requirements-gebco.txt   # exact pins
-e .[gebco]
