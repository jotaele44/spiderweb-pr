# EarthGPT iOS — Satellite Anomaly Detection Engine
# Dependencies are defined as the `earthgpt` extra in pyproject.toml.
#
#   pip install -r requirements-earthgpt.txt
#
# Package: earthgpt/   Stage runners: scripts/
# Optimized for iOS / a-Shell (no GDAL, no heavy GIS stack).
-e .[earthgpt]
